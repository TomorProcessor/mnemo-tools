## 1. Foundation: runtime helper and mode detection

- [ ] 1.1 Add `get_i18n_mode(project_path)` helper to `lib/set_orch/profile_loader.py`. Reads `set/plugins/project-type.yaml`, returns `"single"` only when the file exists AND `i18n.mode` is exactly `"single"`. All other cases (file missing, no `i18n` block, no `mode` key, unknown value) return `"multi"`. Cache results per `project_path`. Log a warning when the value is present but unrecognized [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback]
- [ ] 1.2 Unit-test `get_i18n_mode()` for all five branches (single, multi, file missing, block missing, unknown value) in `tests/unit/test_web_template_i18n_modes.py` [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback]
- [ ] 1.3 Document the helper in `docs/architecture/profile-system.md` (or the closest existing architecture doc) — one paragraph describing the contract and the default-multi fallback [REQ: backwards-compatibility-for-projects-predating-this-capability]

## 2. Seed files: typed copy module and rule

- [ ] 2.1 Create `modules/web/set_project_web/templates/nextjs/src/copy/index.ts` with an `as const` typed copy module containing the same namespaces currently seeded in `messages/en.json` (home, nav, footer, common). Use English string values; the operator's `--primary-locale` choice does NOT alter these values (it sets `PRIMARY_LOCALE` but copy text stays English by default — operators translate as needed) [Layer 2 — module] [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location]
- [ ] 2.2 Create `modules/web/set_project_web/templates/nextjs/src/copy/locale.ts` exporting a `PRIMARY_LOCALE` constant with placeholder value `"en-US"` (the deploy step substitutes the actual value at init time) [Layer 2 — module] [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module]
- [ ] 2.3 Create `modules/web/set_project_web/templates/nextjs/rules/copy-module-conventions.md` documenting the typed-copy pattern: how to add strings, how to use `PRIMARY_LOCALE` with `Intl.*`, the migration story to multi-mode if ever needed, and the boundary where strings legitimately stay outside `src/copy/` (log lines, error messages thrown to stderr, etc.) [Layer 2 — module] [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location]

## 3. Manifest module split

- [ ] 3.1 Update `modules/web/set_project_web/templates/nextjs/manifest.yaml`: remove `rules/i18n-conventions.md`, `messages/hu.json`, `messages/en.json`, `scripts/check-i18n-completeness.ts` from the `core:` block [Layer 2 — module] [REQ: multi-mode-seeded-files]
- [ ] 3.2 Add a new `modules.i18n-multi:` block in the manifest containing the four files removed in 3.1, with description "Multi-locale i18n machinery (next-intl + sidecar pattern)" [Layer 2 — module] [REQ: multi-mode-seeded-files]
- [ ] 3.3 Add a new `modules.i18n-single:` block in the manifest containing `rules/copy-module-conventions.md`, `src/copy/index.ts`, `src/copy/locale.ts`, with description "Single-locale typed copy module" [Layer 2 — module] [REQ: single-mode-seeded-files]
- [ ] 3.4 Verify (via test 8.4 below) that no other manifest entries inadvertently include i18n-related files in `core:` [Layer 2 — module] [REQ: single-mode-seeded-files]

## 4. CLI integration: `--i18n` and `--primary-locale` flags

- [ ] 4.1 Add `--i18n` argument to `lib/set_project_base/cli.py` `deploy-templates` subcommand. Accepts exactly `single` or `multi`. Default: `single`. Reject any other value with a non-zero exit and an error message naming the valid values [Layer 1 — core] [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type]
- [ ] 4.2 Add `--primary-locale` argument to `lib/set_project_base/cli.py` `deploy-templates` subcommand. Accepts a BCP-47-shaped string (regex-validated: `^[a-z]{2}(-[A-Z]{2})?$`). Default: `en-US`. Reject malformed values with a non-zero exit [Layer 1 — core] [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module]
- [ ] 4.3 In `set-project init`'s wrapper layer (where `deploy_templates` is called), translate `--i18n single` to a `--modules i18n-single` addition and `--i18n multi` to a `--modules i18n-multi` addition. Compose with any operator-supplied `--modules` list (additive, not replacement) [Layer 1 — core] [REQ: cli-flag-flow-into-manifest-module-selection]
- [ ] 4.4 Detect and reject contradictory selections (e.g., `--i18n single --modules i18n-multi`) at CLI parse time with a non-zero exit and a message naming the conflict [Layer 1 — core] [REQ: cli-flag-flow-into-manifest-module-selection]
- [ ] 4.5 Default the web template to `--i18n single` so an operator running `set-project init --project-type web --template nextjs` (with no i18n flag) gets single-mode seeding. Make the default explicit in argparse `default="single"` rather than fall-through behavior [Layer 1 — core] [REQ: default-mode-is-single]

## 5. Init-time overlay generation

- [ ] 5.1 In `lib/set_orch/profile_deploy.py`, after `deploy_templates()` finishes file deployment, write the `i18n:` block into `set/plugins/project-type.yaml`. The block contains: `mode` (the chosen value), `primary_locale` (the chosen value), and (single mode only) `copy_root: src/copy` [Layer 1 — core] [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type]
- [ ] 5.2 In single mode, also write the overlay keys: `disabled_rules: [i18n-completeness]` and `disabled_directives: [no-parallel-i18n, consolidate-i18n]`. If `set/plugins/project-type.yaml` already contains those keys (e.g., from a previous init or operator edit), MERGE additively rather than overwrite [Layer 1 — core] [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives]
- [ ] 5.3 In multi mode, do NOT add any of the i18n entries to `disabled_rules` or `disabled_directives`. If those entries already exist from a prior init (operator-edited), leave them — but warn at INFO level that multi mode is paired with disabled i18n machinery (likely a misconfiguration) [Layer 1 — core] [REQ: multi-mode-overlay-leaves-i18n-rules-and-directives-enabled]
- [ ] 5.4 In single mode, perform a search-and-replace on the deployed `src/copy/locale.ts` to substitute the chosen `--primary-locale` value into the `PRIMARY_LOCALE` constant. The substitution is a literal replace of the placeholder string `"en-US"` (default seed) with the operator-supplied locale, anchored to the `PRIMARY_LOCALE = "..."` line to avoid collateral edits [Layer 1 — core] [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module]
- [ ] 5.5 In single mode, write `cross_cutting_files.i18n: ["src/copy/index.ts", "src/copy/locale.ts"]` into `project-knowledge.yaml`. In multi mode, write `cross_cutting_files.i18n: ["messages/*.json"]`. Use the existing additive YAML merge semantics in `profile_deploy.py:_merge_yaml_additive` (keys present from a prior deploy are preserved; new key added) [Layer 1 — core] [REQ: cross_cutting_files-i18n-reflects-mode]

## 6. Runtime hardening: defense-in-depth

- [ ] 6.1 In `lib/set_orch/dispatcher.py:_detect_i18n_sidecar`, short-circuit to `""` immediately if `get_i18n_mode(wt_path) == "single"`. The current code already returns `""` when no `messages/` directory is present, so this is defense-in-depth only — explicit is clearer than relying on a side-effect of file absence. Add a debug log: `logger.debug("_detect_i18n_sidecar: skipping (i18n.mode=single) for %s", wt_path)` [Layer 1 — core] [REQ: i18n-sidecar-pattern-for-parallel-agents]
- [ ] 6.2 In `lib/set_orch/design_manifest.py` (the hygiene scanner that owns rule #2 "Hardcoded UI strings"), branch on `get_i18n_mode(project_path)`: in multi mode, scan strings against `messages/*.json` and label findings "i18n leakage" (today's behavior); in single mode, scan strings against `src/copy/index.ts` (parsing the `as const` object literal with a forgiving regex of `"key": "value"` pairs) and label findings "copy-module leakage" [Layer 1 — core] [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns]
- [ ] 6.3 In the same scanner, skip rule #9 ("Locale-prefix inconsistency") entirely when `get_i18n_mode() == "single"` (single mode has no `[locale]` route segment, so the rule is meaningless). Log a debug line on skip [Layer 1 — core] [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns]
- [ ] 6.4 Verify the existing `i18n_check` gate (`modules/web/set_project_web/gates.py:execute_i18n_check_gate`) continues to gracefully skip single-mode projects via its existing path (no `messages/` dir → "no messages/ directory"; no `useTranslations` usage → "no useTranslations/getTranslations usage"). Add a regression test asserting both skip paths return `"skipped"` for a single-mode fixture [Layer 2 — module] [REQ: single-mode-seeded-files]
- [ ] 6.5 Verify the existing ESLint `no-literal-string` rule (`modules/web/set_project_web/templates/nextjs/eslint.config.mjs:47`) continues to be a no-op in single mode because its `files` matcher is scoped to `src/app/[locale]/**`, which does not exist. No code change required; add a comment to the eslint config noting the mode-aware behavior [Layer 2 — module] [REQ: single-mode-seeded-files]

## 7. Runner scripts: explicit `--i18n multi` for multi-locale fixtures

- [ ] 7.1 Audit `tests/e2e/runners/*.sh` for any runner that bootstraps a multi-locale fixture. Identify runners that today rely on the implicit-multi behavior [REQ: backwards-compatibility-for-projects-predating-this-capability]
- [ ] 7.2 Update each identified runner script to pass `--i18n multi` explicitly to its `set-project init` invocation. Same for `--primary-locale` if the fixture targets a non-default locale [REQ: default-mode-is-single]
- [ ] 7.3 Document the requirement in `tests/e2e/README.md`: "Runners that produce multi-locale apps MUST pass `--i18n multi` explicitly. Default is single." [REQ: default-mode-is-single]
- [ ] 7.4 If the project's CI grep-checks runner scripts (per the Risks section of the design doc), add a check: any runner invoking `set-project init --project-type web` should also pass an explicit `--i18n` flag. If no such CI check exists today, document the expectation in the README rather than introducing new CI scaffolding (out of scope) [REQ: default-mode-is-single]

## 8. Tests

- [ ] 8.1 In `tests/unit/test_web_template_i18n_modes.py`, add a fixture that runs `deploy_templates()` for `--i18n single` into a tmp dir, then asserts: `src/copy/index.ts` exists, `src/copy/locale.ts` exists, `.claude/rules/copy-module-conventions.md` exists, `messages/en.json` does NOT exist, `messages/hu.json` does NOT exist, `scripts/check-i18n-completeness.ts` does NOT exist, `.claude/rules/i18n-conventions.md` does NOT exist [REQ: single-mode-seeded-files]
- [ ] 8.2 Add a parallel test for `--i18n multi` asserting the inverse (multi files present, copy-module files absent) [REQ: multi-mode-seeded-files]
- [ ] 8.3 Add a test asserting the `set/plugins/project-type.yaml` overlay contents per mode: single mode contains `i18n.mode: single`, `disabled_rules: [i18n-completeness]`, `disabled_directives: [no-parallel-i18n, consolidate-i18n]`; multi mode contains `i18n.mode: multi` with no i18n entries in disabled lists [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives]
- [ ] 8.4 Add a test asserting the resolver's behavior with each overlay: `ProjectTypeResolver.resolve_rules()` for single-mode overlay does NOT return the `i18n-completeness` rule; for multi-mode overlay it DOES. Same for `resolve_directives()` and `no-parallel-i18n` / `consolidate-i18n` [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives]
- [ ] 8.5 Add a test for `get_i18n_mode()` covering: single, multi explicit, file missing, block missing, unknown value (each returns the documented value AND the unknown case logs a warning) [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback]
- [ ] 8.6 Add a test for `_detect_i18n_sidecar()` returning `""` in single mode, regardless of whether `next-intl` is in `package.json` (the explicit short-circuit added in 6.1) [REQ: i18n-sidecar-pattern-for-parallel-agents]
- [ ] 8.7 Add a test for the hygiene scanner: in single-mode mode, a hardcoded JSX string not present in `src/copy/index.ts` produces a "copy-module leakage" WARN finding; the same string when present in `src/copy/index.ts` produces no finding. Add the inverse for multi mode (existing behavior, regression coverage) [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns]
- [ ] 8.8 Add a test for the CLI's flag validation: invalid `--i18n` values are rejected; contradictory `--i18n single --modules i18n-multi` is rejected; valid combinations succeed [REQ: cli-flag-flow-into-manifest-module-selection]
- [ ] 8.9 Add a test for the `--primary-locale` flag substitution: after deploy, `src/copy/locale.ts` contains the chosen locale string [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module]
- [ ] 8.10 Add a test for backwards compatibility: a `set/plugins/project-type.yaml` with no `i18n:` block makes `get_i18n_mode()` return `"multi"` and the resolver returns the full rule + directive set unchanged [REQ: backwards-compatibility-for-projects-predating-this-capability]
- [ ] 8.11 Update existing tests in `modules/web/tests/test_i18n_check_gate.py` and `modules/web/tests/test_i18n_checker_scope.py` to add multi-mode fixtures explicitly and add new single-mode fixtures asserting the gate's existing skip path is hit (regression coverage) [Layer 2 — module] [REQ: single-mode-seeded-files]

## 9. End-to-end verification

- [ ] 9.1 Run `set-project init --i18n single --primary-locale en-US` against a tmp fixture; sanity-check the deployed file tree by hand using a checklist matching 8.1's assertions [REQ: single-mode-seeded-files]
- [ ] 9.2 Run a full orchestration cycle (planner → dispatch → verify → merge) against a single-mode fixture with a small spec ("create a home page with a CTA button"). Verify: no `messages/` directory created at any point, no `i18n_check` gate fires, no `no-parallel-i18n` directive applied to the plan, no sidecar instructions appear in any agent's input.md, agent's component code references `copy.home.cta` (or similar) [REQ: single-mode-seeded-files]
- [ ] 9.3 Run a full orchestration cycle against a multi-mode fixture (the existing craftbrew runner with `--i18n multi` explicit). Verify behavior is identical to today's runs: same gate count, same directive count, same dispatch context shape; sidecar files created and merged on archive [REQ: multi-mode-seeded-files]
- [ ] 9.4 Diff the multi-mode run's orchestration log against the most recent pre-change craftbrew run; assert no behavioral regression in the i18n machinery (gate timings ±10%, directive applications equal, merge outcomes equal) [REQ: backwards-compatibility-for-projects-predating-this-capability]
- [ ] 9.5 Run `set-design-hygiene` on each fixture and inspect the findings file: single-mode fixture surfaces "copy-module leakage" labels (not "i18n leakage"); multi-mode fixture surfaces "i18n leakage" labels (today's behavior); no rule #9 findings in single-mode fixture [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns]

## 10. Documentation

- [ ] 10.1 Update `tests/e2e/README.md` to document the `--i18n` flag, the default, and the requirement that multi-locale runners pass `--i18n multi` explicitly [REQ: default-mode-is-single]
- [ ] 10.2 Update `docs/guide/getting-started.md` (or equivalent operator-facing doc) to mention the `--i18n` choice at init and link to `web-template-i18n-modes` spec for the contract [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type]
- [ ] 10.3 Add a section to the deployed `copy-module-conventions.md` rule explaining how to scale the typed copy module (split into `src/copy/<feature>.ts` files re-exported from `index.ts`) when it grows beyond a few hundred entries [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location]
- [ ] 10.4 Add a brief migration note to `copy-module-conventions.md` describing how an operator can later switch single → multi (manual: install next-intl middleware, serialize copy module to `messages/<primary_locale>.json`, replace `copy.x.y` references with `t('x.y')` via codemod, set mode to multi). Out of scope as automation but documented as a feasible operator-driven path [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location]

## Acceptance Criteria (from spec scenarios)

### web-template-i18n-modes

- [ ] AC-1: WHEN an operator runs `set-project init --project-type web --template nextjs --i18n single` THEN the resulting `set/plugins/project-type.yaml` SHALL contain a top-level `i18n:` block with `mode: single` [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type, scenario: mode-persisted-at-init-time]
- [ ] AC-2: WHEN an operator runs `set-project init --project-type web --template nextjs --i18n multi` THEN the resulting `set/plugins/project-type.yaml` SHALL contain `i18n.mode: multi` [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type, scenario: multi-mode-persisted]
- [ ] AC-3: WHEN an operator passes `--i18n none` or any value other than `single` or `multi` THEN `set-project init` SHALL exit with a non-zero status and an error message naming the valid values [REQ: i18n-mode-is-an-explicit-axis-on-the-web-project-type, scenario: invalid-mode-rejected]
- [ ] AC-4: WHEN an operator runs `set-project init --project-type web --template nextjs` with no `--i18n` flag THEN `set/plugins/project-type.yaml` SHALL contain `i18n.mode: single` AND the project SHALL be seeded with single-mode files [REQ: default-mode-is-single, scenario: default-applied]
- [ ] AC-5: WHEN init completes with `--i18n single` THEN `src/copy/index.ts`, `src/copy/locale.ts`, `.claude/rules/copy-module-conventions.md` SHALL exist [REQ: single-mode-seeded-files, scenario: single-mode-files-present]
- [ ] AC-6: WHEN init completes with `--i18n single` THEN `messages/en.json`, `messages/hu.json`, `scripts/check-i18n-completeness.ts`, `.claude/rules/i18n-conventions.md` SHALL NOT exist [REQ: single-mode-seeded-files, scenario: multi-mode-files-absent-in-single-mode]
- [ ] AC-7: WHEN init completes with `--i18n multi` THEN `messages/en.json`, `messages/hu.json`, `scripts/check-i18n-completeness.ts`, `.claude/rules/i18n-conventions.md` SHALL exist [REQ: multi-mode-seeded-files, scenario: multi-mode-files-present]
- [ ] AC-8: WHEN init completes with `--i18n multi` THEN `src/copy/index.ts`, `src/copy/locale.ts`, `.claude/rules/copy-module-conventions.md` SHALL NOT exist [REQ: multi-mode-seeded-files, scenario: single-mode-files-absent-in-multi-mode]
- [ ] AC-9: WHEN init completes with `--i18n single` THEN `set/plugins/project-type.yaml` SHALL contain `disabled_rules` listing at least `i18n-completeness` AND `disabled_directives` listing at least `no-parallel-i18n` and `consolidate-i18n` [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives, scenario: disabled-lists-present-in-single-mode]
- [ ] AC-10: WHEN `ProjectTypeResolver.resolve_rules()` is called for a single-mode project THEN the returned rule list SHALL NOT include the `i18n-completeness` rule [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives, scenario: resolver-honors-single-mode-overlay]
- [ ] AC-11: WHEN `ProjectTypeResolver.resolve_directives()` is called for a single-mode project THEN the returned directive list SHALL NOT include `no-parallel-i18n` or `consolidate-i18n` [REQ: single-mode-overlay-disables-i18n-specific-rules-and-directives, scenario: resolver-honors-single-mode-overlay-directives]
- [ ] AC-12: WHEN init completes with `--i18n multi` AND `ProjectTypeResolver.resolve_rules()` is called THEN the rule list SHALL include `i18n-completeness` [REQ: multi-mode-overlay-leaves-i18n-rules-and-directives-enabled, scenario: multi-mode-preserves-rules]
- [ ] AC-13: WHEN init completes with `--i18n multi` AND `ProjectTypeResolver.resolve_directives()` is called THEN the directive list SHALL include both `no-parallel-i18n` and `consolidate-i18n` [REQ: multi-mode-overlay-leaves-i18n-rules-and-directives-enabled, scenario: multi-mode-preserves-directives]
- [ ] AC-14: WHEN init completes with `--i18n single` THEN `project-knowledge.yaml` `cross_cutting_files.i18n` SHALL be `["src/copy/index.ts", "src/copy/locale.ts"]` [REQ: cross_cutting_files-i18n-reflects-mode, scenario: single-mode-cross-cutting-list]
- [ ] AC-15: WHEN init completes with `--i18n multi` THEN `project-knowledge.yaml` `cross_cutting_files.i18n` SHALL be `["messages/*.json"]` [REQ: cross_cutting_files-i18n-reflects-mode, scenario: multi-mode-cross-cutting-list]
- [ ] AC-16: WHEN an agent's change modifies `src/copy/index.ts` in a single-mode project THEN the `cross-cutting-review` directive SHALL flag the change [REQ: cross_cutting_files-i18n-reflects-mode, scenario: cross-cutting-review-directive-remains-active-in-single-mode]
- [ ] AC-17: WHEN init completes with `--i18n single` and no `--primary-locale` flag THEN `set/plugins/project-type.yaml` SHALL contain `i18n.primary_locale: en-US` AND `src/copy/locale.ts` SHALL export `PRIMARY_LOCALE = "en-US"` [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module, scenario: default-locale-used]
- [ ] AC-18: WHEN init completes with `--i18n single --primary-locale hu-HU` THEN `set/plugins/project-type.yaml` SHALL contain `i18n.primary_locale: hu-HU` AND `src/copy/locale.ts` SHALL export `PRIMARY_LOCALE = "hu-HU"` [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module, scenario: custom-locale-captured]
- [ ] AC-19: WHEN init completes with `--i18n multi --primary-locale hu-HU` THEN the locale value SHALL be recorded in `set/plugins/project-type.yaml` AND SHALL NOT alter any seeded file [REQ: primary-locale-is-captured-at-init-and-propagated-to-the-copy-module, scenario: locale-ignored-in-multi-mode]
- [ ] AC-20: GIVEN `set/plugins/project-type.yaml` contains `i18n.mode: single` WHEN `get_i18n_mode(project_path)` is called THEN it SHALL return `"single"` [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback, scenario: helper-returns-single]
- [ ] AC-21: GIVEN `set/plugins/project-type.yaml` contains `i18n.mode: multi` WHEN `get_i18n_mode(project_path)` is called THEN it SHALL return `"multi"` [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback, scenario: helper-returns-multi-when-explicitly-set]
- [ ] AC-22: GIVEN `set/plugins/project-type.yaml` does not exist WHEN `get_i18n_mode(project_path)` is called THEN it SHALL return `"multi"` [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback, scenario: helper-falls-back-to-multi-when-file-missing]
- [ ] AC-23: GIVEN `set/plugins/project-type.yaml` exists but contains no `i18n:` block WHEN `get_i18n_mode(project_path)` is called THEN it SHALL return `"multi"` [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback, scenario: helper-falls-back-to-multi-when-block-absent]
- [ ] AC-24: GIVEN `set/plugins/project-type.yaml` contains `i18n.mode: none` (or any unknown value) WHEN `get_i18n_mode(project_path)` is called THEN it SHALL return `"multi"` and log a warning naming the unknown value [REQ: get-i18n-mode-project_path-runtime-helper-with-default-multi-fallback, scenario: helper-falls-back-to-multi-for-unknown-values]
- [ ] AC-25: GIVEN a project initialized before this change with `set/plugins/project-type.yaml` containing only `type: web` and `template: nextjs` WHEN the orchestration engine runs THEN all current i18n verification rules and orchestration directives SHALL be applied [REQ: backwards-compatibility-for-projects-predating-this-capability, scenario: pre-existing-project-keeps-multi-behavior]
- [ ] AC-26: GIVEN a pre-existing project with populated `messages/en.json` and `messages/hu.json` WHEN an agent dispatches a change THEN the dispatch context SHALL include i18n sidecar instructions AND the verify pipeline SHALL include the `i18n_check` gate [REQ: backwards-compatibility-for-projects-predating-this-capability, scenario: pre-existing-project-with-messages-keeps-gates-working]
- [ ] AC-27: WHEN a component renders a user-visible string in a single-mode project THEN the source SHALL contain `copy.<namespace>.<key>` rather than a hardcoded string literal [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location, scenario: component-uses-typed-copy]
- [ ] AC-28: WHEN a single-mode component formats a date or currency THEN the source SHALL pass `PRIMARY_LOCALE` to the `Intl.*` or `toLocale*` call [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location, scenario: locale-aware-intl-call-uses-primary_locale]
- [ ] AC-29: WHEN strings are added to `src/copy/index.ts` THEN they SHALL be organized in nested namespaces matching the structure that `messages/<locale>.json` would use [REQ: single-mode-typed-copy-module-is-the-canonical-ui-string-location, scenario: migration-ready-structure-preserved]
- [ ] AC-30: WHEN `set-project init --i18n single --modules integrations` is invoked THEN the deployed manifest modules SHALL be exactly `{i18n-single, integrations}` [REQ: cli-flag-flow-into-manifest-module-selection, scenario: single-mode-adds-i18n-single-module]
- [ ] AC-31: WHEN `set-project init --i18n multi --modules integrations` is invoked THEN the deployed manifest modules SHALL be exactly `{i18n-multi, integrations}` [REQ: cli-flag-flow-into-manifest-module-selection, scenario: multi-mode-adds-i18n-multi-module]
- [ ] AC-32: WHEN an operator passes both `--i18n single` and `--modules i18n-multi` THEN `set-project init` SHALL exit with a non-zero status and an error message naming the conflict [REQ: cli-flag-flow-into-manifest-module-selection, scenario: conflict-raises-error]

### i18n-conventions

- [ ] AC-33: GIVEN a project with `set/plugins/project-type.yaml` containing `i18n.mode: multi` WHEN the engine evaluates compliance with the i18n-conventions spec THEN every requirement defined in this spec SHALL be evaluated against the project [REQ: applicability, scenario: multi-mode-project-subject-to-these-requirements]
- [ ] AC-34: GIVEN a project with `set/plugins/project-type.yaml` containing `i18n.mode: single` WHEN the engine evaluates compliance with i18n-conventions THEN every requirement SHALL be skipped [REQ: applicability, scenario: single-mode-project-not-subject-to-these-requirements]
- [ ] AC-35: GIVEN a project with `set/plugins/project-type.yaml` containing no `i18n.mode` key WHEN the engine evaluates compliance THEN every requirement SHALL be evaluated [REQ: applicability, scenario: legacy-project-treated-as-multi-mode]
- [ ] AC-36: GIVEN a multi-mode project WHEN a component renders a user-visible string THEN the source SHALL invoke `t('namespace.key')` rather than embed a hardcoded literal [REQ: req-i18n-translation-keys, scenario: multi-mode-component-uses-translation-key]
- [ ] AC-37: GIVEN a single-mode project WHEN a component renders a user-visible string THEN the i18n-conventions translation-keys requirement SHALL NOT apply (the typed copy module requirement applies instead) [REQ: req-i18n-translation-keys, scenario: single-mode-component-exempt]
- [ ] AC-38: GIVEN a multi-mode project's `src/i18n/request.ts` imports a sidecar file WHEN the import is evaluated THEN the import expression SHALL be inside a try/catch block [REQ: req-i18n-sidecar-resilience, scenario: multi-mode-sidecar-import-is-wrapped]
- [ ] AC-39: GIVEN a single-mode project WHEN the engine evaluates the sidecar-resilience requirement THEN the requirement SHALL NOT apply [REQ: req-i18n-sidecar-resilience, scenario: single-mode-project-has-no-sidecars]
- [ ] AC-40: GIVEN a multi-mode project with a language switcher WHEN the switcher is rendered THEN it SHALL use `<Link>` with locale prop rather than a button with `router.replace` [REQ: req-i18n-language-switcher, scenario: multi-mode-language-switcher-uses-link]
- [ ] AC-41: GIVEN a single-mode project WHEN the engine evaluates the language-switcher requirement THEN the requirement SHALL NOT apply [REQ: req-i18n-language-switcher, scenario: single-mode-project-has-no-language-switcher]
- [ ] AC-42: GIVEN a multi-mode project rendering a dynamic-route Link WHEN the Link points to a parameterized pathname THEN the `href` SHALL be `{ pathname, params }` [REQ: req-i18n-dynamic-routes, scenario: multi-mode-dynamic-route-uses-object-href]
- [ ] AC-43: GIVEN a single-mode project WHEN the engine evaluates the dynamic-routes requirement THEN the requirement SHALL NOT apply [REQ: req-i18n-dynamic-routes, scenario: single-mode-project-does-not-use-next-intl-pathnames]
- [ ] AC-44: GIVEN a multi-mode project's `playwright.config.ts` WHEN the config is loaded THEN `use.locale` SHALL be set to a non-empty BCP-47 string [REQ: req-i18n-e2e-locale, scenario: multi-mode-playwright-config-sets-locale]
- [ ] AC-45: GIVEN a single-mode project's `playwright.config.ts` WHEN the config is loaded THEN this requirement SHALL NOT apply (single mode follows web-template-i18n-modes guidance instead) [REQ: req-i18n-e2e-locale, scenario: single-mode-playwright-config-follows-primary_locale]
- [ ] AC-46: GIVEN a multi-mode project's `src/middleware.ts` WHEN the matcher is evaluated THEN it SHALL exclude all `/api` paths [REQ: req-i18n-middleware, scenario: multi-mode-middleware-excludes-api-routes]
- [ ] AC-47: GIVEN a single-mode project WHEN the engine evaluates the middleware requirement THEN the requirement SHALL NOT apply [REQ: req-i18n-middleware, scenario: single-mode-project-has-no-i18n-middleware]

### cross-cutting-file-strategy

- [ ] AC-48: WHEN a change is dispatched in a multi-mode project that touches i18n keys AND the project uses JSON-based i18n THEN the dispatch context SHALL instruct the agent to write keys to a sidecar file [REQ: i18n-sidecar-pattern-for-parallel-agents, scenario: agent-receives-sidecar-instructions-in-multi-mode]
- [ ] AC-49: WHEN the planner creates changes in a multi-mode project that each need i18n keys THEN each change SHALL be assigned specific top-level namespaces [REQ: i18n-sidecar-pattern-for-parallel-agents, scenario: namespace-assignment-in-multi-mode]
- [ ] AC-50: WHEN an agent in a multi-mode project adds i18n keys following dispatch instructions THEN the keys SHALL be in a separate file that does not conflict with other agents' i18n files [REQ: i18n-sidecar-pattern-for-parallel-agents, scenario: agent-writes-to-sidecar-in-multi-mode]
- [ ] AC-51: WHEN a change is dispatched in a single-mode project THEN the dispatch context SHALL NOT include any i18n sidecar instructions AND the agent SHALL be directed to add user-visible strings to `src/copy/index.ts` [REQ: i18n-sidecar-pattern-for-parallel-agents, scenario: single-mode-project-receives-no-sidecar-instructions]
- [ ] AC-52: WHEN a branch in a multi-mode project containing sidecar files is merged to main THEN the merger SHALL combine all sidecar files into the canonical `en.json` (and other locale files) [REQ: post-merge-i18n-combination, scenario: sidecar-files-merged-after-branch-merge-in-multi-mode]
- [ ] AC-53: WHEN the merged branch does not contain i18n sidecar files (regardless of mode) THEN the merger SHALL skip the combination step without error [REQ: post-merge-i18n-combination, scenario: no-sidecar-files-present]
- [ ] AC-54: WHEN two sidecar files in a multi-mode project define the same top-level namespace key THEN the combination step SHALL report a warning and use last-write-wins ordering [REQ: post-merge-i18n-combination, scenario: namespace-collision-detected-in-multi-mode]
- [ ] AC-55: WHEN a branch in a single-mode project is merged THEN the merger's i18n combination step SHALL be a no-op [REQ: post-merge-i18n-combination, scenario: single-mode-project-skips-combination-step]
- [ ] AC-56: GIVEN a single-mode project initialized via `set-project init --i18n single` WHEN `project-knowledge.yaml` is inspected THEN `cross_cutting_files.i18n` SHALL be `["src/copy/index.ts", "src/copy/locale.ts"]` [REQ: single-mode-treats-src-copy-index-ts-as-a-cross-cutting-file, scenario: single-mode-project-knowledge-points-at-copy-module]
- [ ] AC-57: GIVEN a multi-mode project initialized via `set-project init --i18n multi` WHEN `project-knowledge.yaml` is inspected THEN `cross_cutting_files.i18n` SHALL be `["messages/*.json"]` [REQ: single-mode-treats-src-copy-index-ts-as-a-cross-cutting-file, scenario: multi-mode-project-knowledge-points-at-messages]
- [ ] AC-58: WHEN a change in a single-mode project modifies `src/copy/index.ts` THEN the `cross-cutting-review` directive SHALL flag the change for extra review [REQ: single-mode-treats-src-copy-index-ts-as-a-cross-cutting-file, scenario: cross-cutting-review-fires-on-copy-edits-in-single-mode]
- [ ] AC-59: WHEN multiple changes in a single-mode project's planning round all need to modify `src/copy/index.ts` THEN the planner SHALL apply the existing cross-cutting ownership rule [REQ: single-mode-treats-src-copy-index-ts-as-a-cross-cutting-file, scenario: planner-serializes-copy-module-edits-in-single-mode]

### design-source-hygiene

- [ ] AC-60: GIVEN `v0-export/components/search-palette.tsx` declares `const MOCK_PRODUCTS = [...]` WHEN `set-design-hygiene` runs THEN the output contains a CRITICAL finding [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: mock-array-detected]
- [ ] AC-61: GIVEN 10 of 24 pages import `SiteHeader` and 14 do not WHEN the scanner runs THEN a CRITICAL finding lists the 14 pages without import [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: header-inconsistency-detected]
- [ ] AC-62: GIVEN `<Link href="/loginnn">` in a TSX file AND the manifest has no `/loginnn` route THEN a CRITICAL finding fires AND suggests the closest match [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: broken-route-detected]
- [ ] AC-63: GIVEN a multi-mode project with `<Button>Kosárba</Button>` in a component AND the string is not present in any `messages/*.json` file WHEN scanner runs THEN a WARN finding fires labelled "i18n leakage" [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: hardcoded-hu-string-detected-multi-mode]
- [ ] AC-64: GIVEN a single-mode project with `<Button>Add to cart</Button>` AND the string is not present in `src/copy/index.ts` WHEN scanner runs THEN a WARN finding fires labelled "copy-module leakage" [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: hardcoded-string-in-single-mode-is-checked-against-copy-module]
- [ ] AC-65: GIVEN a single-mode project with `<Button>{copy.cart.add}</Button>` AND `src/copy/index.ts` contains `cart: { add: "Add to cart" }` WHEN scanner runs THEN no finding fires for this string [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: single-mode-string-referenced-via-copy-module-is-not-a-finding]
- [ ] AC-66: GIVEN a multi-mode project where a Hungarian-prefixed page imports an English-prefixed-only path WHEN scanner runs THEN a WARN finding fires for rule #9 [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: locale-prefix-inconsistency-detected-in-multi-mode]
- [ ] AC-67: GIVEN a single-mode project (no `[locale]` route segment) WHEN scanner runs THEN rule #9 SHALL produce no findings [REQ: 9-hygiene-rules-detect-common-design-source-antipatterns, scenario: locale-prefix-rule-skipped-in-single-mode]
