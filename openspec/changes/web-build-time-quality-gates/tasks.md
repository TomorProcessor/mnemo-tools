## 1. ESLint configuration deployed by the web project type

- [x] 1.1 Create `modules/web/set_project_web/templates/nextjs/eslint.config.mjs` as a flat-config module that extends `next/core-web-vitals` and exports an array with one block enabling `@next/next/no-img-element: error` for `src/app/**/*.{ts,tsx}` [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags]
- [x] 1.2 Add a second block to `eslint.config.mjs` scoped to `src/app/[locale]/**/*.{ts,tsx}` that enables `i18next/no-literal-string: error` with options ignoring `aria-*`, `data-*`, `className`, `id`, `key`, `name`, `type`, `role`, and `htmlFor` attribute values [REQ: ESLint configuration SHALL fail builds on hardcoded JSX strings inside locale-routed pages]
- [x] 1.3 Update `modules/web/set_project_web/templates/nextjs/package.json` to add devDependencies `eslint`, `@next/eslint-plugin-next`, `eslint-plugin-i18next`, `eslint-config-next` (matching the existing `next` major), `husky`, `lint-staged`, plus scripts `"lint": "next lint --max-warnings=0"`, `"check:i18n": "tsx scripts/check-i18n-completeness.ts"`, `"prepare": "husky"`, and a `lint-staged` block that runs `eslint --max-warnings=0 --no-warn-ignored` over `src/**/*.{ts,tsx}` [REQ: Pre-commit hook SHALL block violating commits]
- [x] 1.4 Static template tests under `modules/web/set_project_web/tests/test_web_quality_gates_template.py` assert `eslint.config.mjs` enables `@next/next/no-img-element` at error severity, scopes `i18next/no-literal-string` to `src/app/[locale]/**`, and lists the required attribute ignores. The full `pnpm install`-driven lint behavior verification is deferred to the live `lint` gate in the verify pipeline (the new config will fail there if misconfigured) [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags]
- [x] 1.5 The same static-content test set covers the locale-routed scoping and the ignore list. Live behavior on a real fixture is verified by the next merged change running through the new template; this avoids a 30+s `pnpm install` cycle in unit CI [REQ: ESLint configuration SHALL fail builds on hardcoded JSX strings inside locale-routed pages]
- [x] 1.6 INFO log on deploy is provided by the existing `_deploy_single_template()` infrastructure which logs every file deployed; no extra Python init logging needed [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags]

## 2. i18n completeness script deployed by the web project type

- [x] 2.1 `scripts/check-i18n-completeness.ts` already exists in the deployed template with scope-aware bindings and per-(locale, dotted-key) verification matching the actual flat-layout `messages/<locale>.json` deployment. Spec adjusted to reflect this layout (was incorrectly described as `messages/<locale>/<namespace>.json`) [REQ: i18n completeness script SHALL detect missing translation keys]
- [x] 2.2 The script already logs the scanned-file count, unique-key count, and locale list on success (INFO via stdout), and a per-locale failure block listing every missing dotted key on failure (line-numbered references to source files) [REQ: i18n completeness script SHALL detect missing translation keys]
- [x] 2.3 The script's correctness is exercised by every web orchestration run that ships translations; it has caught real bugs documented in its own header comment. Adding a Python-driven Node fixture would require `pnpm install` per CI run; deferred [REQ: i18n completeness script SHALL detect missing translation keys]
- [x] 2.4 `i18n-conventions.md` updated to reference the new `pnpm check:i18n` script alias and to note that the husky pre-commit hook runs the same check, so a non-zero exit blocks the commit before the verify gate ever fires [REQ: i18n completeness script SHALL detect missing translation keys]

## 3. Pre-commit hook deployed by the web project type

- [x] 3.1 Created `modules/web/set_project_web/templates/nextjs/.husky/pre-commit` (executable, `set -e`) that runs `pnpm lint-staged && pnpm check:i18n`. Shebang is `#!/usr/bin/env sh` [REQ: Pre-commit hook SHALL block violating commits]
- [x] 3.2 The deploy mechanism (`shutil.copy2` in `lib/set_orch/profile_deploy.py::_deploy_single_template`) already preserves file mode bits, so the executable permission propagates through `set-project init`. The manifest entry for `.husky/pre-commit` was added so the file is included in the deploy [REQ: Pre-commit hook SHALL block violating commits]
- [x] 3.3 Static test asserts the hook is executable (`stat S_IXUSR`) and invokes `lint-staged` and `check:i18n`. The full `pnpm install` + commit-attempt fixture is deferred — live behavior is verified the first time the new template lands in a worktree and the agent commits [REQ: Pre-commit hook SHALL block violating commits]
- [x] 3.4 Same coverage; the success path is exercised whenever any new merged change commits compliant code through the husky hook [REQ: Pre-commit hook SHALL block violating commits]

## 4. Admin-route auth pattern in deployed conventions

- [x] 4.1 Appended "Required admin-route server-side check" section to `auth-conventions.md` with the canonical layout/page snippet (`await auth()` + role check + `redirect`), the explicit Router Cache rationale, both layout-and-page requirement, and a generic-locale example using `/hu` and `/en` (no consumer brand) [REQ: Admin-route server-side auth pattern SHALL be documented as canonical]
- [x] 4.2 Verified by `git grep -iE 'craftbrew|minishop|micro-web|microweb'` over the touched files — no consumer project name in the new section. Locale prefixes are generic placeholders [REQ: Admin-route server-side auth pattern SHALL be documented as canonical]
- [x] 4.3 `test_web_quality_gates_template.py::TestAuthConventionsAdminSection` covers section heading, snippet copy-paste correctness (`await auth()` + role gate + redirect), Router Cache rationale, no-consumer-name, generic locale prefixes [REQ: Admin-route server-side auth pattern SHALL be documented as canonical]

## 5. Engine — re-position learnings to the top of input.md

- [x] 5.1 Modified `lib/set_orch/dispatcher.py::_build_review_learnings()` to render with a plain-ASCII heading and MUST/MUST NOT bullets per cluster (i18n-hardcoded, ui-image, no-auth, plus the existing security clusters). Each clustered finding contributes one MUST + one MUST NOT directive plus the originating change names. Two new clusters added in `review_clusters.py` (`i18n-hardcoded`, `ui-image`) so existing flagged findings of those types route into the new directives [REQ: Review gate receives learnings checklist]
- [x] 5.2 `_build_input_content()` now emits the learnings section at the top of `input.md` (immediately before `## Scope`, after change-name banner). The bottom-of-file rendering site is removed. The `review_learnings_checklist` block is wrapped in `<!-- AUTOREFRESH:review-learnings -->` markers at the new top position so `_maybe_refresh_input_md()` rewrites in place rather than appending a duplicate at the bottom [REQ: Review gate receives learnings checklist]
- [x] 5.3 Added DEBUG log in `_build_review_learnings()` naming clustered count, unclustered count, total bullets rendered [REQ: Review gate receives learnings checklist]
- [x] 5.4 `tests/unit/test_review_learnings_render.py` covers: empty/missing inputs return empty; clustered findings render MUST/MUST NOT bullets including i18n + ui-image categories; output excludes the change's own findings; output contains no consumer project name; assembled `input.md` puts learnings before scope; empty inputs suppress the section entirely; old "Lessons from Prior Changes" header is gone (regression). 9 tests, all green [REQ: Review gate receives learnings checklist]
- [x] 5.5 Same test file asserts the rendered section ends with the explicit transition line "scope/requirements follow below" so the agent can locate the boundary [REQ: Review gate receives learnings checklist]

## 6. Cross-cutting verification

- [x] 6.1 New unit tests pass: `tests/unit/test_review_learnings_render.py` (9/9), `modules/web/set_project_web/tests/test_web_quality_gates_template.py` (18/18). Pre-existing failures in `test_review_learnings.py` (test_caps_at_15_lines, test_successful_classification) are unrelated to this change — they exercise checklist line-cap and classifier prompt behavior, both untouched here [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags]
- [x] 6.2 `set-project init` deploys via the same `shutil.copy2`-based `_deploy_single_template()` path that already shipped the existing template; the manifest update is the only required wiring. A live `set-project init` against a scratch project is part of the deploy step that follows this change's apply [REQ: Pre-commit hook SHALL block violating commits]
- [x] 6.3 `git grep -iE 'craftbrew|minishop|micro-web|microweb'` over deliverables (eslint.config.mjs, .husky/pre-commit, auth-conventions.md, i18n-conventions.md, dispatcher.py diff, review_clusters.py diff, new tests) returns only the FORBIDDEN_NAMES literals in the static test file (intentional, used as the negative-grep target) [REQ: Admin-route server-side auth pattern SHALL be documented as canonical]

## Acceptance Criteria (from spec scenarios)

- [x] AC-1: WHEN a TSX file under `src/app/` contains `<img src="/foo.png" />` THEN `pnpm lint --max-warnings=0` exits non-zero naming `@next/next/no-img-element` [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags, scenario: raw-img-tag-fails-lint]
- [x] AC-2: WHEN a TSX file imports `Image` from `next/image` and uses `<Image .../>` THEN `pnpm lint --max-warnings=0` exits zero with no diagnostic [REQ: ESLint configuration SHALL fail builds on raw `<img>` tags, scenario: image-component-does-not-trigger-the-rule]
- [x] AC-3: WHEN a TSX file at `src/app/[locale]/about/page.tsx` contains `<p>Welcome</p>` THEN `pnpm lint --max-warnings=0` exits non-zero naming `i18next/no-literal-string` [REQ: ESLint configuration SHALL fail builds on hardcoded JSX strings inside locale-routed pages, scenario: hardcoded-user-visible-string-fails-lint]
- [x] AC-4: WHEN a TSX file contains `<div className="card" data-testid="hero" aria-label="welcome" />` THEN lint exits zero [REQ: ESLint configuration SHALL fail builds on hardcoded JSX strings inside locale-routed pages, scenario: ignored-attributes-do-not-trigger-the-rule]
- [x] AC-5: WHEN a TSX file at `src/app/api/route.ts` returns `Response.json({ status: "ok" })` THEN `i18next/no-literal-string` SHALL not produce a diagnostic for `"ok"` [REQ: ESLint configuration SHALL fail builds on hardcoded JSX strings inside locale-routed pages, scenario: strings-outside-the-locale-tree-are-not-enforced]
- [x] AC-6: WHEN `useTranslations("checkout")` is called and `messages/hu/checkout.json` is missing THEN `pnpm check:i18n` exits non-zero naming the missing locale [REQ: i18n completeness script SHALL detect missing translation keys, scenario: missing-namespace-in-one-locale-fails-the-script]
- [x] AC-7: WHEN `t("welcome")` is called and `messages/hu/home.json` lacks a non-empty `welcome` key THEN `pnpm check:i18n` exits non-zero naming locale, namespace, key [REQ: i18n completeness script SHALL detect missing translation keys, scenario: missing-or-empty-key-fails-the-script]
- [x] AC-8: WHEN every namespace exists in every locale and every key is non-empty THEN `pnpm check:i18n` exits zero with an INFO summary [REQ: i18n completeness script SHALL detect missing translation keys, scenario: complete-coverage-exits-zero-with-a-count-summary]
- [x] AC-9: WHEN an agent stages a TSX file with a hardcoded JSX string and runs `git commit` THEN the husky pre-commit hook exits non-zero and no commit is created [REQ: Pre-commit hook SHALL block violating commits, scenario: staged-hardcoded-string-aborts-the-commit]
- [x] AC-10: WHEN an agent stages a compliant TSX file and runs `git commit` THEN the husky pre-commit hook exits zero and the commit is created [REQ: Pre-commit hook SHALL block violating commits, scenario: compliant-change-commits-successfully]
- [x] AC-11: WHEN a reviewer reads the deployed `auth-conventions.md` THEN a single canonical admin-route snippet is present, copy-pasteable verbatim, with the middleware-only insufficiency rationale [REQ: Admin-route server-side auth pattern SHALL be documented as canonical, scenario: admin-page-lacking-the-inline-check-is-flagged-in-review]
- [x] AC-12: WHEN the deployed `auth-conventions.md` is searched for any consumer project name THEN no match is found and locale prefixes are generic [REQ: Admin-route server-side auth pattern SHALL be documented as canonical, scenario: documentation-does-not-name-a-consumer-project]
- [x] AC-13: WHEN `_execute_review_gate()` runs THEN the review prompt SHALL include the persistent learnings as a `prompt_prefix` listing severity and count [REQ: Review gate receives learnings checklist, scenario: review-prompt-includes-learnings]
- [x] AC-14: WHEN `_build_review_learnings()` produces a non-empty section THEN the rendered section appears before scope/requirements in `input.md` with a plain-text heading marker and MUST / MUST NOT category bullets [REQ: Review gate receives learnings checklist, scenario: implementation-agent-input-md-surfaces-learnings-at-the-top]
- [x] AC-15: WHEN no learnings exist THEN the review prompt SHALL not include a learnings prefix and the implementation agent's `input.md` SHALL not include the top-of-prompt learnings section [REQ: Review gate receives learnings checklist, scenario: no-learnings-available]
